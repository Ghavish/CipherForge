{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m agents.manager & python -m agents.system_architect & python -m agents.ui_coder & python -m agents.backend_coder & python -m agents.reviewer & python -m agents.mergemaster",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}